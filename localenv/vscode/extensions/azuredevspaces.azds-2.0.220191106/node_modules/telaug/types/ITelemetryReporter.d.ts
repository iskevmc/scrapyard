export interface ITelemetryReporter {
    sendTelemetryEvent: (eventName: string, properties?: {
        [key: string]: string;
    }, measurements?: {
        [key: string]: number;
    }) => void;
}
