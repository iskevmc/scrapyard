import { ITelemetryEvent } from './ITelemetryEvent';
import { Result } from './Result';
export interface ITimedEvent extends ITelemetryEvent {
    markTime: (markName: string, fromStart?: boolean) => number;
    end: (result: Result, message?: string, sendNow?: boolean) => number;
    endIfNotSent: () => void;
}
